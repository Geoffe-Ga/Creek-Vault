"""Canonical, version-controlled templates deployed by ``creek init``.

The vault scaffold under :mod:`creek.templates.vault` and the
schema-skill tree under :mod:`creek.templates.skills` are the single
source of truth for what a fresh Creek vault looks like. ``creek
init`` materialises them into a user-chosen vault path; ``creek
skills sync`` re-deploys the skill tree alone after upstream updates.
See FEAT-019.
"""
