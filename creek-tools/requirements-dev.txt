# Development dependencies (delegate to pyproject.toml extras).
#
# pyproject.toml [project.optional-dependencies] is the source of truth
# for the dev toolchain — including type stubs. This file pulls in [dev]
# and [all] so a single `pip install -r requirements-dev.txt` provisions
# a complete dev environment with every optional code path available
# for testing.
-e .[all,dev]

# Pinned transitive bumps to address pip-audit findings (DEP-003).
# These are transitive dependencies whose vulnerable older versions can
# leak in via pip's resolver. See ./scripts/security.sh for documented
# ignores.
#   - idna<3.15: CVE-2026-45409.
cryptography>=48.0.0
idna>=3.15
pip>=26.1.1
pyjwt>=2.12.1
setuptools>=78.1.1
wheel>=0.47.0
