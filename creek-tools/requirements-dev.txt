# Development dependencies (delegate to pyproject.toml extras).
#
# NOTE: `uv.lock` is the canonical, fully-pinned environment — install
# with `uv sync --all-extras` for a reproducible local/CI environment.
# This file is a best-effort fallback for plain-pip users and is NOT
# pinned, so it can drift from the lock.
#
# pyproject.toml [project.optional-dependencies] is the source of truth
# for the dev toolchain — including type stubs. This file pulls in [dev]
# and [all] so a single `pip install -r requirements-dev.txt` provisions
# a complete dev environment with every optional code path available
# for testing. Security floors for transitive dependencies (DEP-003)
# live in pyproject.toml: runtime pins under [project.dependencies] and
# build/dev pins under [tool.uv] constraint-dependencies.
-e .[all,dev]
